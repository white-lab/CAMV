version: Build {build}

skip_non_tags: true

# build:
#     verbosity: minimal

environment:
    matrix:
        - nodejs_version: "6"

install:
    - ps: Install-Product node $env:nodejs_version
    - "cd src/"
    - "npm install"
    - "npm install bower -g"
    - "bower install"
    - "cd .."

# test_script:
#     - npm test

build_script:
    - src\node_modules\.bin\electron-packager src/ --all

artifacts:
    - path: "CAMV-darwin-x64"
    - path: "CAMV-linux-ia32"
    - path: "CAMV-linux-x64"
    - path: "CAMV-mas-x64"
    - path: "CAMV-win32-ia32"
    - path: "CAMV-win32-x64"

deploy:
    - provider: GitHub
      description: ""
      artifact: "CAMV-*"
      draft: false
      prerelease: false
      on:
        appveyor_repo_tag: true
      auth_token:
        secure: eD410Uh2srKjC/H5V/J1q8xLeMZQuo2ChyO5RiSlAq1wsmlkQhvqfRfoMed2/ZOk
